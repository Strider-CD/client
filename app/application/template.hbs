<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class='navbar-brand'}}Strider{{/link-to}}
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li>{{#link-to 'projects'}}Projects{{/link-to}}</li>
        <li>{{#link-to 'drones'}}Drones{{/link-to}}</li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          {{#if session.guestlogin}}
            <li><a {{ action 'invalidateSession' }}>Logout</a></li>
          {{/if}}
        {{else}}
          <li>{{#link-to 'login' nonguest=true}}Login{{/link-to}}</li>
        {{/if}}
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      {{outlet}}
    </div>
  </div>
</div>
